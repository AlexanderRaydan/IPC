#include <stdio.h>
#include <stdbool.h>

int main(void)
{
    // You must use this variable in #3 data analysis section!
    // int x = 10;
    // int y = x++;

    // printf("x   : %d\n", x);

    // printf("y   : %d\n", y);
    // printf("++y : %d\n", ++y);

    // bool manaos = true;
    // bool manaodont = false;

    // printf("%d\n", manaos);    // Returns 1 (true)
    // printf("%d\n", manaodont); // Returns 1 (true)

    // int i, j, k;

    // i = 5;
    // j = 10;

    // k = ++i + j--;

    // printf("i = %d\nj = %d\nk = %d\n", i, j, k);

    printf("%d\n", ('L' == 'L'));
    printf("%d\n", ('L' == 'l'));

    return 0;
}
